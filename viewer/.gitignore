node_modules/
dist/
# The root .gitignore ignores package-lock.json repo-wide; re-include it here so the
# distributable widget builds from a locked, reproducible dependency tree.
!package-lock.json
# Playwright artifacts
test-results/
playwright-report/
.playwright/
# Basis transcoder is vendored out of node_modules at build time (scripts/copy-basis.mjs).
public/basis/
# Large sample assets for local dev / e2e verification (not committed).
public/fixtures/
*.log
